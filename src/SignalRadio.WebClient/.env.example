# Development: dev server proxies /api to http://localhost:5000 by default
# Set VITE_API_BASE to change the base URL used by the client at runtime
# Example: VITE_API_BASE=http://localhost:5000

# VITE_API_BASE=
