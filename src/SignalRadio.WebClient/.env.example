# Development: dev server proxies /api to http://localhost:5000 by default
# Set VITE_API_BASE to change the base URL used by the client at runtime
# Example: VITE_API_BASE=http://localhost:5000

# VITE_API_BASE=

# Optional: set a simple admin password for the client admin page. This is a
# client-side check only. For production, secure the API endpoint as well.
# VITE_ADMIN_PASSWORD=mysecret
