# SignalRadio Environment Configuration

# Azure Storage Configuration
# For development with Azure Storage Emulator:
#AzureStorage__ConnectionString=UseDevelopmentStorage=true

# For production with real Azure Storage Account:
# AzureStorage__ConnectionString=DefaultEndpointsProtocol=https;AccountName=your_account;AccountKey=your_key;EndpointSuffix=core.windows.net

# Optional: Override container name
# AzureStorage__ContainerName=recordings-dev

# Optional: Override path pattern
# AzureStorage__DefaultPathPattern={SystemName}/{TalkgroupId}/{Year}/{Month}/{Day}

# API Configuration
ASPNETCORE_ENVIRONMENT=Development
API_ENDPOINT=http://signalradio-api:8080/api/recording/upload
StorageType=Local

# Logging Configuration
LOG_LEVEL=Information

# Host paths used by docker-compose.override.yml to bind recordings volumes
# Set these to absolute paths if you prefer, e.g. /srv/signalradio/api-recordings
# API_RECORDINGS_HOST_PATH=./data/recordings
# TRUNK_RECORDINGS_HOST_PATH=./data/trunk-recordings

# AI Transcript Summarization (Azure OpenAI)
# Set SEMANTIC_KERNEL_ENABLED=true to enable AI summaries
SEMANTIC_KERNEL_ENABLED=false
# AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/
# AZURE_OPENAI_KEY=your-azure-openai-api-key-here
# AZURE_OPENAI_DEPLOYMENT=gpt-4
# SEMANTIC_KERNEL_MAX_TOKENS=1500
# SEMANTIC_KERNEL_TEMPERATURE=0.3
# SEMANTIC_KERNEL_CACHE_DURATION=30
# SEMANTIC_KERNEL_DEFAULT_WINDOW=60